TODO
====

- slow loading of ManageIngredients, ManageRecipes and ManageShoppingList. TBD:
    * ManageShoppingList

- Amount: Possibility to add ranges? (e.g. 1-2 Peperoni, 200-250g Fleisch, ...)
        -> Nein, dafür erlauben, "-" in Amounts anzugeben. Dann wird es NICHT addiert.
        -> Das lässt sich nicht sinnvoll implementieren. Trotzdem andere Variante?
- Alternativen in Rezepten (e.g. Poulet oder Tofu)! Wie könnte dies gehandhabt werden?
    -> Im Rezept als Gruppe zusammen definiert und beim Einfügen in die ShoppingList muss man dann eines davon auswählen!
        (macht evtl. das Laden dann noch langsamer, da komplexes Layout...)
        -> Dann müsste ich es halt besser optimieren :p
- Amount: improve getChangeAmount-Method

- data synchronization: improve UI and possibly the code itself (UPDATING OF FILE LISTS ON CHANGES!)
- Data synchronisation - part 2:
    2. merge-functionality (-> "data consistency check"?)
    3. two-way auto-sync with merge
    4. [optional] Connect to Google App Folder API / Google App Engine / Amazon Web Services / Dropbox datastore API / something similar (or some of them)
