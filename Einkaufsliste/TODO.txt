TODO
====

- Braucht es für DelRecipe zuerst eine abfrage (aka "wirklich löschen?")? Brauchte es dies irgendwo sonst (auch) noch?

- Annotations / remarks for shoppinglist items? (d.h. zusatzinformationen)
- Possibility to add ranges? (e.g. 1-2 Peperoni, 200-250g Fleisch, ...)
- Alternativen in Rezepten (e.g. Poulet oder Tofu)! Wie könnte dies gehandhabt werden?
- Auswahl "Anzahl" durch +/- Buttons statt durch direkte Texteingabe? Analoge Änderungen für andere Einheiten!
    -> Oder ggfs. einfach ein paar Standardwerte zur schnelleren Auswahl?
    -> Oder +/- Buttons auch e.g. für Gramm, welche jeweils um \pm 50 den Wert verändern (+ immer noch die Möglichkeit, manuell eine Zahl einzugeben)
- Sometimes you don't want things to get add up together (e.g. 250g Geschnetzeltes für X und 500g für Essen Y. Aufteilen der Verpackung ist aber doof).
    -> Was tun? Vorschlag nicole: in go shopping hinten am Text noch irgendwie hinschreiben (250g + 500g) bei zusmmengefassten entries.
- data synchronization: improve UI and possibly the code itself (UPDATING OF FILE LISTS ON CHNAGES!)

- slow loading of some activites. Check what can be done to improve speed:
    * ManageIngredients
    * ManageShoppingList
    * ManageRecipes (list of recipeitems is loaded noticeably later than the rest of the UI)
    => one problem is that getSortedItemsList or similar methods should be cached within the adapter! (the should in principle know, when the items get changed, or should this be done by an observer-like pattern on the ingredients etc. classes themselves?
    => might another problem be because of the somewhat complex layouts?
- decide how to handle saving now and adjust accordingly!
    -> saving of einkaufsliste.json would then happen in a background thread (needs proper locking!), but when should this be started? Timer? When quiting an activity?
            (when already doing this: ensure, that manually saving cannot overwrite einkaufsliste.json!)
- Data synchronisation - part 2:
    2. merge-functionality (-> "data consistency check"?)
    3. two-way auto-sync with merge
    4. [optional] Connect to Google App Folder API / Google App Engine / Amazon Web Services / Dropbox datastore API / something similar (or some of them)
